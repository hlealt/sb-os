---
name: sb-task
description: Use whenever the user mentions a task or a task file (*-tasks.md), or work requires creating, listing, reading, editing, rescheduling, reprioritizing, completing, reopening, or deleting a vault task — including marking a task wip/done when starting or finishing work, ticking subtasks, and managing task dependencies. Runs the sb-task CLI instead of hand-editing task markdown. Routing of NEW tasks to the right file stays with sb-vault-ops; this skill executes the operations.
---

Read and execute `{sb_os_path}/para/cli/sb-task/sb-task.md`.
