<!--
  Copy-ready skeleton for {wiki_root}/purpose.md — the OPTIONAL ingest focus lens.

  OPTIONAL: absent → lens OFF → /sb-wiki-ingest behaves identically to today.
  ENABLE:  copy this file to {wiki_root}/purpose.md and replace every placeholder with your own focus.
  DISABLE: delete or rename {wiki_root}/purpose.md → lens OFF instantly.

  Keep ONLY the lines below the line break when you copy. Replace each `<...>` placeholder.
  Delete the `<!-- guidance: ... -->` comments once you have written your content.
  The five H2 headings are the parsing contract — keep all five, do not rename them.
  Lens mechanics + classification bands: `wiki/docs/wiki-schema.md` § "Regulatory layer — purpose.md".
-->

---
type: purpose
last-touched: YYYY-MM-DD
---

# Wiki Purpose

## Mission
<one to three sentences: why this wiki exists and the lens sources are read through>
<!-- guidance: State the wiki's reason for existing and the angle you read sources through. Sets the frame; not parsed as a match set. -->

## Focus areas
<!-- guidance: The match set for in-focus classification. Ranked, scoped domains to synthesize MORE deeply and bias topic/stub suggestions toward. One bullet per area: a short name plus a one-line scope note. Optional tiering via `### Primary` / `### Secondary` subheadings. -->
- **<area name>** — <one-line scope note>
- **<area name>** — <one-line scope note>

## Down-weight signals
<!-- guidance: Source shapes or subjects to treat with LESS discretionary depth — never dropped, never below baseline. One bullet per signal: the shape plus why it earns less depth. -->
- <signal> — <why>

## Quality bar
<!-- guidance: Editorial preferences applied while synthesizing a source page (e.g., favor primary sources; mechanism over hype; prefer dated and sourced claims). Does NOT influence the wiki sources index `What it says` phrasing. -->
- <preference>

## Out of purpose
<!-- guidance: Subjects matching NO focus area → trigger the Stage-1 off-purpose flag (advisory; ingest still proceeds if you choose). List explicit off-purpose subjects, or leave implicit: "anything not in Focus areas." -->
<anything not in Focus areas>
